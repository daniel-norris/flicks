module.exports = {
    future: {
        // removeDeprecatedGapUtilities: true,
        // purgeLayersByDefault: true,
    },
    purge: [],
    theme: {
        extend: {
            gridTemplateRows: {
                // layout specific
                'layout': 'auto 80vh auto',
            }
        },
    },
    variants: {},
    plugins: [],
}
